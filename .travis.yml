sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t shashidhar/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true shashidhar/react-test npm test

after_success:
  - docker build -t shashidhar/multi-client ./client
  - docker build -t shashidhar/multi-nginx ./nginx
  - docker build -t shashidhar/multi-server ./server
  - docker build -t shashidhar/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push shashidhar/multi-client
  - docker push shashidhar/multi-nginx
  - docker push shashidhar/multi-server
  - docker push shashidhar/multi-worker
